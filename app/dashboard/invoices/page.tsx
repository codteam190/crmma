import InvoiceClient from '@/components/InvoiceClient';

export const metadata = {
  title: 'Invoices & Billing | CRM',
};

export default function InvoicesPage() {
  return (
    <div>
      <InvoiceClient />
    </div>
  );
}