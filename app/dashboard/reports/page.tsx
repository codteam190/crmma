import DailyReportClient from '@/components/DailyReportClient';

export const metadata = {
  title: 'Reports & Analytics | CRM',
};

export default function ReportsPage() {
  return (
    <div>
      <DailyReportClient />
    </div>
  );
}